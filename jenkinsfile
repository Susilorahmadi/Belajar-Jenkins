pipeline {
    agent any
    stages {
        stage('http') {
            steps {
                echo "using httpRequest"'
                httpRequest consoleLogResponseBody: true, url: 'https://reqres.in/api/users'
                echo "using curl"'
                bat curl https://reqres.in/api/users'
            }
        }
    }
}
